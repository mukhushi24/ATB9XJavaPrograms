package Nov.ex__15112024WrapperException;

public class Lab_170Exception {
   // exception disturbs the program normal flow
    //if we don't handle the exception...control is passed to jvm and jvm terminates the program
    //error can't be handled(out of memory),exception can be handled...
    //object->throwable->errors,exception
    //exception->checked(JVM knows it)(at compile time JVM cry's saying to handle Exception),
    // unchecked(JVM doesn't know it)(at compile time nothing happens)at runtime we will know that there is exception
    //file,sql,input IO->jvm knows


}
